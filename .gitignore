.DS_Store
xcuserdata/
*.xcuserstate
build/
DerivedData/
# Le cache d'Xcode, quand on le range dans le dépôt : il contient des liens
# symboliques circulaires (les checkouts SPM), que le scan de sources de
# Tailwind suit jusqu'à faire tomber le build du site.
.derivedData/
.swiftpm/
__pycache__/
node_modules/
.next/
.vercel/
*.tsbuildinfo

# Écrits par `next dev` à chaque lancement (`generate-agent-files.js`). Ce sont des notes
# pour les agents, régénérées depuis la version de Next installée : suivies, elles
# apparaissent dans des diffs qui ne parlent pas d'elles.
AGENTS.md
CLAUDE.md
